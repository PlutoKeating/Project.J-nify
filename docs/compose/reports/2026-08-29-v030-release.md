---
feature: v030-jennifer-full
status: released
specs:
  - docs/compose/specs/2026-08-29-jennifer-agent-complete-spec.md
authority: docs/DECISION_REGISTER.md
---

# J-nify v0.3.0（Jennifer Agent 完整实现）验收报告

> 范围：官方文档集与 system prompt 装配（热重载）、MCP 风格本地数据原文、结构化记忆与用户记忆文档、客户端会话持久化、SSE 流式对话、数据改动卡片与一键撤销、admin 文档/记忆/playground/成本管理面、本地引擎消费节奏策略。
> 验收结论：**后端 + App + 官网/文档三端交付完成并已上线**（Release `v0.3.0`，versionCode=6）。

## 一、交付范围与实际完成情况

| 模块 | 计划项 | 状态 | 说明 |
| --- | --- | --- | --- |
| 后端 | 官方文档集 `agent_docs`（identity/workflow/tools + 任意 skill/custom md） | ✅ | admin CRUD/排序/启停，保存即热重载；system prompt 按序装配（文档集 → 时间上下文 → 新会话记忆文档） |
| 后端 | 结构化记忆 `agent_memories` + 用户记忆文档 | ✅ | 类型 preference/fact/event/lesson；`memory_read/write/delete` 工具；新会话注入一次；workflow 文档含记忆沉淀强制条款 |
| 后端 | 工具集扩展至 15 个 | ✅ | guardrails_set / feedback_read / steps_get/set / memory 三件套 / draft_generate LLM 化；items_delete 二次确认；移除 items_create 的 max_nudges 硬编码 |
| 后端 | MCP 风格上下文原文 | ✅ | chat `context`（设备本地日历/天气/usage/窗口摘要）完整原文拼入 prompt，不落库不记日志，4KB 上限；history role 白名单 |
| 后端 | SSE 流式 | ✅ | start/tool/delta/done/error；LLM 网关支持 OpenAI 兼容流式 + 故障切换 |
| 后端 | 数据改动留痕与撤销 | ✅ | `agent_action_logs`（before/after）+ `POST /v1/jennifer/undo`（24h 逆操作） |
| 后端 | 节奏下发 | ✅ | `GET /v1/rhythm` 供本地引擎消费 |
| 后端 | 调用日志与成本看板 | ✅ | `agent_call_logs` 落库 + admin `/api/costs`；告警自动评估维持手动（R6） |
| 后端 | admin 管理面 | ✅ | `/api/docs*`、`/api/memories*`、`/api/playground`、`/api/costs` + SPA 四个新板块 |
| App | 会话持久化 / 流式 / Markdown / 占位气泡 | ✅ | sqflite 只存文本消息；SSE 增量渲染；`flutter_markdown`；responding 占位 |
| App | 改动卡片 + 一键撤销 | ✅ | 活跃会话内纯前端卡片（R9：不注入上下文、不入消息记录、退出即失效） |
| App | 本地引擎消费节奏 | ✅ | 按类目拉取 `GET /v1/rhythm`，替换硬编码 72h 冷却 |
| 官网/文档 | 状态与路线图如实更新 | ✅ | M1/M2 已发布、护栏口径修正、README/HANDOVER/API/ARCHITECTURE/JENNIFER_AGENT_REPORT 同步 |

## 二、验证结果

| 项 | 结果 |
| --- | --- |
| 后端 | `npm run typecheck` 全绿；`npm test` **79 passed / 5 skipped**（新增 agent-full.test.ts 5 例：文档装配顺序/记忆分区编译/热重载/历史白名单全链路） |
| 前端 | `flutter analyze` **0 issues**；`flutter test` **11 passed** |
| 官网 | `npm run build` 通过；**21 tests 通过** |
| 数据库 | 生产 Supabase 应用迁移 `20260829000001_jennifer_full.sql`（agent_docs/agent_memories/agent_action_logs/agent_call_logs + 三件套种子，RLS 全开） |
| 部署 | 后端 Worker 生产 URL 验证；官网 Pages 验证；v0.3.0 APK/AAB 经 release 流水线发布 |

## 三、发布记录

- v0.3.0（2026-08-29，Release `v0.3.0`）：`pubspec` `0.3.0+6`（versionCode=6 > 历史最大 5）；后端 `APP_VERSION=0.3.0`。

## 四、遗留与人工验证项

1. **真机冒烟（用户）**：对话流式渲染、Markdown 展示、发送后占位气泡、改动卡片撤销、重启 App 后会话保留、新会话后 Jennifer 能复述记忆。
2. **admin 冒烟（管理员）**：编辑 identity 文档 → 不重新部署、新会话立即生效；playground 查看装配后 system prompt；成本看板有数据。
3. **告警自动评估**：维持手动测试通道（R6 定案），`agent_call_logs` 数据已具备，后续接 cron 即可复用。

## 五、已定案暂不做

外部 MCP 运行时（R10）、会话入服务端库（R4）、向量检索（后置）、skill 外部路径导入（R11，本期粘贴 md）、告警自动评估（R6）。详见 `docs/compose/specs/2026-08-29-jennifer-agent-complete-spec.md` §13。
