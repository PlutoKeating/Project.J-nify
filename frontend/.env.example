# J-nify frontend — .env.example
# 后端地址等配置完全由 .env 控制（便于运维与版本控制；.env 本身不入库）。
# 复制为 .env 后按需修改。
# 生产上线环境唯一 Base URL：https://j-nify.williamhvollita.dpdns.org
# （代码默认值即生产地址；本地开发请把 BACKEND_BASE_URL 指到你的后端，如 http://localhost:8000）
BACKEND_BASE_URL=http://localhost:8000
APP_ENV=development
API_TIMEOUT=15

# Supabase 端点（认证 / 会话）：值也可用 --dart-define=SUPABASE_URL=... 注入（.env 优先）。
# SUPABASE_ANON_KEY 是 publishable（anon）key，绝不填 service role key。
# 本地 supabase CLI：URL http://localhost:54321，anon key 见 supabase status。
#SUPABASE_URL=http://localhost:54321
#SUPABASE_ANON_KEY=your-supabase-anon-key

# 天气/地图（本地处理，真值不提交；release 构建用 --dart-define 注入）
OPENWEATHER_API_KEY=
# 天地图 Key 为服务端配置（CF Secret TIANDITU_KEY，经 /v1/geo/reverse 代理），App 不再持有
