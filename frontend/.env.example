# J-nify frontend — .env.example
# 后端地址等配置完全由 .env 控制（便于运维与版本控制；.env 本身不入库）。
# 复制为 .env 后按需修改。
# 生产上线环境唯一 Base URL：https://jnify.williamhvollita.dpdns.org
# （代码默认值即生产地址；本地开发请把 BACKEND_BASE_URL 指到你的后端，如 http://localhost:8000）
BACKEND_BASE_URL=http://localhost:8000
APP_ENV=development
API_TIMEOUT=15
